<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_bg"
    android:orientation="vertical" >
    <!-- <LinearLayout 
        android:id="@+id/feedback_opinion_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/feedback_opinion_bg_shape"
        android:clickable="true"
        android:layout_margin="10dp"
        android:orientation="vertical"
        > -->
        <EditText 
            android:id="@+id/feedback_opinion_text"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:layout_gravity="top"
            android:gravity="top"
            android:textSize="16sp"
            android:background="@drawable/feedback_opinion_bg_shape"
            android:layout_margin="10dp"
            android:hint="@string/feedback_opinion"
            />
    <!--</LinearLayout>  -->
    
	<EditText 
	    android:id="@+id/feedback_contact_text"
	    android:layout_width="match_parent"
	    android:layout_height="wrap_content"
	    android:background="@drawable/feedback_contact_bg_shape"
	    android:maxLines="1"
        android:layout_margin="10dp"
	    android:textSize="16sp"
	    android:hint="@string/feedback_contact"
	    />
	
	<Button 
	    android:id="@+id/feedback_commit_btn"
	    android:layout_width="match_parent"
	    android:layout_height="wrap_content"
	    android:layout_margin="10dp"
	    android:background="@drawable/feedback_commit_bg_shape"
	    android:text="@string/commit"
	    android:textColor="@color/white"
	    android:textSize="16sp"
	    />

</LinearLayout>
